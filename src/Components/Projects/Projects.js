import React from "react";
import { StyledBackground, StyledContentParagraph } from "./styles.js";

function Projects() {
  // use logos/image of some sort to get links to heroku pages?
  // when someone goes to this page, send a wake up call to each of the pages for faster load time?
  // carousel with an image from the project and a short blurb?
  // create card component for carousel? look up how people create that
  // get screenshots & links for projects
  return (
    <>
      {/* <StyledBackground>
        <StyledContentParagraph>
          <h1>Projects</h1>
          <p>I know stuff!</p>
        </StyledContentParagraph>
      </StyledBackground> */}
    </>
  );
}

export default Projects;
